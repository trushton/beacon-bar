import React from 'react';
import ReactDOM from 'react-dom';
import Bartender from './VR_admin';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<VR_Admin />, div);
});
